{# {{ form_start(form) }}
<div class="row">
	<div class="col-md-12">
		{{ form_row(form.commandeMedicaments) }}
	</div>
</div>
<div class="d-flex-gap-1">
	<button class="btn btn-outline-primary">{{ button_label|default('Sauvegarder') }}</button>
	<a class="btn btn-success" href="{{ path('app_commande_index') }}">back to list</a>
</div>

{{ form_end(form) }} #}
