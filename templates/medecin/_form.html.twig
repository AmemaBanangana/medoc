{{ form_start(form) }}
<div class="row">
	<div class="col-md-6">
		{{ form_row(form.nom) }}
		{{ form_row(form.prenom) }}
	</div>
	<div class="col-md-6">
		{{ form_row(form.adresse) }}
		{{ form_row(form.phone) }}
	</div>
</div>
<div class="d-flex gap-1">
<button class="btn btn-outline-primary">{{ button_label|default('Sauvegarder') }}</button>
<a class="btn btn-primary" href="{{ path('app_medecin_index') }}">retour a la liste</a>
</div>
{{ form_end(form) }}
